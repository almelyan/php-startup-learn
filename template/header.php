<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Nearby School</title>

    <!-- Bootstrap -->
    <link href="https://fonts.googleapis.com/css?family=Roboto+Mono" rel="stylesheet">
    <link href="css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Move down content because we have a fixed navbar that is 50px tall */
        *, body { font-family: 'Roboto Mono', monospace; }
        body { padding-top: 50px;  padding-bottom: 20px; }
        body > .container { margin-top: 20px; }
        .page-header, h1 { margin-top: 0; } h3 { margin-top: 10px; }
        .input-group { width: 100%; margin-bottom: 10px; }
        .input-group-addon { text-align: right; width: 20%; }
        .jumbotron {
            background-size: cover;
            background-repeat: no-repeat;
            background-position-x: center;
            background-position-y: bottom;
        }
        .jumbotron h1 {
            background-color: #222;
            color: #FFF;
            display: inline-block;
            padding: 5px 10px;
            border-radius: 8px;
        }
        #main-jumbotron{ background-image: url('images/bg.jpg'); background-position-y: center; }
        .table th { font-weight: normal; color: #FFF; background-color: #1b809e; }
    </style>


    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>
<body>